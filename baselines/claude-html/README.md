# Claude HTML baseline

`TonalDepth Dashboard (offline).html` is an immutable evidence archive copied from the authoritative input. It is not production source and must not be edited into a package.

The checksum and provenance are recorded in `baseline.json`. Run the Phase 0 audit to verify the archive and regenerate inventories.

